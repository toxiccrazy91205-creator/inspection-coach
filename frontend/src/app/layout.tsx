import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Health Inspection Coach — Ahmedabad",
  description:
    "Predict any Ahmedabad restaurant's FSSAI inspection risk before the inspector shows up. Powered by environmental risk analysis and FSSAI Schedule 4 compliance data.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
